const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const rate = 1.5; const userId = '300067ff-7cde-47be-a5e2-66ff9fc7eb8b'; await prisma.$executeRawUnsafe('UPDATE incomes SET amount = amount * $1 WHERE user_id = $2', rate, userId); console.log('success'); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
