const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const user = await prisma.user.findFirst(); const rate = 1.5; await prisma.$executeRawUnsafe('UPDATE incomes SET amount = amount * $1 WHERE user_id = $2', rate, user.id); const income = await prisma.income.findFirst({ where: { userId: user.id } }); console.log(income.amount); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
